import { sampleAction } from './actions/auth';
import { ActionType } from 'typesafe-actions';

// ================================================= initialState =============================================
export type AuthInitialStateType = {
  count: number;
};
// ================================================= Actions =============================================
export type AuthActionTypes = ActionType<typeof sampleAction>;

// ================================================= initialState =============================================
// ================================================= initialState =============================================
// ================================================= initialState =============================================
// ================================================= initialState =============================================
